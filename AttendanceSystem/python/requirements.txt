# ─────────────────────────────────────────────
#  Facial Recognition Microservice
#  Python >= 3.10 required
# ─────────────────────────────────────────────

# --- Web framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0         # ASGI server (async, production-ready)

# --- Computer Vision & Face Recognition ---
opencv-python-headless==4.9.0.80  # headless: no GUI deps (server environment)
face_recognition==1.3.0           # high-level API over dlib
dlib==19.24.4                     # required by face_recognition
numpy==1.26.4

# --- Image handling ---
Pillow==10.3.0                    # decode images from bytes / base64

# --- Configuration & Validation ---
pydantic==2.7.1
pydantic-settings==2.2.1          # BaseSettings with env-var support

# --- Utilities ---
python-multipart==0.0.9           # multipart/form-data uploads (FastAPI)
httpx==0.27.0                     # async HTTP client (health checks / integration tests)

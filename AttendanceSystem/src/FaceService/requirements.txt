# ─────────────────────────────────────────────────────────────
#  FaceService — Dependencias del Motor de Reconocimiento Facial
#
#  IMPORTANTE — Windows:
#    NO uses "pip install -r requirements.txt" directamente.
#    Usa el script de instalacion:  python install.py
#    (instala insightface con --no-deps para proteger numpy 2.x)
#
#  Versiones de Python soportadas: 3.10, 3.11, 3.12
# ─────────────────────────────────────────────────────────────

# --- Web framework ---
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0

# --- ONNX Runtime ---
onnxruntime>=1.19.0,<1.21.0

# --- numpy: compatible con el wheel de insightface de PyPI (ABI 1.x, 88 bytes) ---
# El wheel oficial de insightface en PyPI fue compilado con numpy 1.x.
# Si se usa el wheel de Gourieff (numpy 2.x), reinstalar con numpy>=2.1.0 manualmente.
numpy>=1.24.0,<2.0

# --- Procesamiento de imagen ---
opencv-python-headless>=4.8.0,<5.0.0

# --- Dependencias de insightface (gestionadas manualmente) ---
# Se instalan ANTES del wheel para evitar que pip las resuelva y baje numpy.
onnx>=1.14.0
scipy>=1.13.0
scikit-image>=0.24.0
Pillow>=10.0.0
tqdm>=4.0.0
requests>=2.28.0
prettytable>=3.0.0
albumentations>=1.4.0
easydict>=1.9
matplotlib>=3.9.0

# --- Configuracion y entorno ---
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
python-dotenv>=1.0,<2.0

# ─────────────────────────────────────────────────────────────
#  FaceService — Dependencias del Motor de Reconocimiento Facial
#
#  IMPORTANTE — Windows:
#    NO uses "pip install -r requirements.txt" directamente.
#    Usa el script de instalacion:  python install.py
#    (resuelve el wheel de insightface para Windows automaticamente)
#
#  Linux/Mac:
#    pip install -r requirements.txt   funciona directo.
#
#  Versiones de Python soportadas: 3.10, 3.11, 3.12
#  Python 3.13 NO esta soportado aun (onnxruntime no tiene wheel estable).
# ─────────────────────────────────────────────────────────────

# --- Web framework ---
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0

# --- Reconocimiento Facial ---
# Windows: instalado via install.py con wheel comunitario
# Linux/Mac: pip install directo funciona
insightface==0.7.3

# --- ONNX Runtime (motor de inferencia) ---
# Usar CPU. Para GPU: reemplazar por onnxruntime-gpu
onnxruntime>=1.18.0,<1.21.0

# --- Procesamiento de imagen ---
numpy>=1.24.0,<2.0.0
opencv-python-headless>=4.8.0,<5.0.0

# --- Configuracion y entorno ---
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
python-dotenv>=1.0,<2.0

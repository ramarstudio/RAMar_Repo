<UserControl x:Class="AttendanceSystem.App.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    
    <Grid Background="#F4F6F9">
        <Border Width="400" Height="450" Background="White" CornerRadius="10" Padding="40" 
                VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.1"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center">
                
                <TextBlock Text="RAMar Studio" FontSize="28" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Control de Asistencia" FontSize="16" Foreground="#7F8C8D" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <TextBlock Text="Usuario" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,5"/>
                <TextBox x:Name="txtUsername" Height="40" Padding="10,0" FontSize="14" Margin="0,0,0,20" 
                         VerticalContentAlignment="Center"/>

                <TextBlock Text="Contraseña" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,5"/>
                <PasswordBox x:Name="txtPassword" Height="40" Padding="10,0" FontSize="14" Margin="0,0,0,10" 
                             VerticalContentAlignment="Center"/>

                <TextBlock x:Name="txtError" Foreground="#E74C3C" FontSize="13" TextWrapping="Wrap" 
                           Visibility="Collapsed" Margin="0,0,0,15" HorizontalAlignment="Center"/>

                <Button x:Name="btnLogin" Content="Ingresar al Sistema" Height="45" FontSize="16" FontWeight="Bold" 
                        Foreground="White" Background="#3498DB" BorderThickness="0" Cursor="Hand"
                        Click="BtnLogin_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
                
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
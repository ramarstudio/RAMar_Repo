<UserControl x:Class="AttendanceSystem.App.Views.Admin.HorariosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="#F0F4F8" Loaded="HorariosView_Loaded">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="28,24,28,28">

            <!-- HEADER -->
            <Border Background="White" CornerRadius="14" Padding="24,18" Margin="0,0,0,18">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.08" Color="#1A237E"/>
                </Border.Effect>
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <Border Width="40" Height="40" CornerRadius="10" Margin="0,0,14,0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#00897B" Offset="0"/>
                                    <GradientStop Color="#00695C" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <materialDesign:PackIcon Kind="CalendarClock" Width="22" Height="22"
                                                     Foreground="White" HorizontalAlignment="Center"
                                                     VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Gestión de Horarios" FontSize="20" FontWeight="Bold" Foreground="#1A237E"/>
                            <TextBlock Text="Asigna horarios personalizados por empleado y día" FontSize="12" Foreground="#78909C"/>
                        </StackPanel>
                    </StackPanel>
                    <Button x:Name="btnNuevo" Click="BtnNuevo_Click"
                            HorizontalAlignment="Right" VerticalAlignment="Center"
                            Height="36" Padding="16,0"
                            Style="{StaticResource MaterialDesignFlatMidBgButton}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" Width="18" Height="18" Margin="0,0,6,0"/>
                            <TextBlock Text="Nuevo Horario"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <!-- FILTRO EMPLEADO -->
            <Border Background="White" CornerRadius="14" Padding="20,16" Margin="0,0,0,18">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.06" Color="#1A237E"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="SELECCIONAR EMPLEADO" FontSize="10" FontWeight="Bold" Foreground="#90A4AE" Margin="0,0,0,10"/>
                    <WrapPanel>
                        <ComboBox x:Name="cmbEmpleado" Width="280"
                                  DisplayMemberPath="Display"
                                  materialDesign:HintAssist.Hint="Empleado"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                  Margin="0,0,12,0"/>
                        <Button x:Name="btnCargar" Click="BtnCargar_Click"
                                Height="40" Padding="20,0"
                                Style="{StaticResource MaterialDesignFlatMidBgButton}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Magnify" Width="18" Height="18" Margin="0,0,6,0"/>
                                <TextBlock Text="Ver Horarios"/>
                            </StackPanel>
                        </Button>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <!-- TABLA HORARIOS -->
            <Border Background="White" CornerRadius="14" Padding="6" Margin="0,0,0,18">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.08" Color="#1A237E"/>
                </Border.Effect>
                <DataGrid x:Name="dgHorarios"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          CanUserSortColumns="True" HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal" BorderThickness="0"
                          Background="White" RowBackground="White"
                          AlternatingRowBackground="#F8FAFD"
                          MaxHeight="350" FontSize="12">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Día" Binding="{Binding Dia}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="#00695C"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Entrada" Binding="{Binding Entrada}" Width="90"/>
                        <DataGridTextColumn Header="Salida" Binding="{Binding Salida}" Width="90"/>
                        <DataGridTextColumn Header="Vigente Desde" Binding="{Binding VigenteDesde}" Width="120"/>
                        <DataGridTextColumn Header="Vigente Hasta" Binding="{Binding VigenteHasta}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- FORMULARIO NUEVO HORARIO -->
            <Border x:Name="pnlFormulario" Background="White" CornerRadius="14" Padding="24,20"
                    Margin="0,0,0,18" Visibility="Collapsed">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.08" Color="#00695C"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="NUEVO HORARIO" FontSize="11" FontWeight="Bold" Foreground="#00897B" Margin="0,0,0,16"/>

                    <WrapPanel>
                        <StackPanel Margin="0,0,16,12">
                            <TextBlock Text="Empleado" FontSize="11" Foreground="#546E7A" Margin="0,0,0,4"/>
                            <ComboBox x:Name="cmbEmpleadoForm" Width="220"
                                      DisplayMemberPath="Display"
                                      Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,12">
                            <TextBlock Text="Día" FontSize="11" Foreground="#546E7A" Margin="0,0,0,4"/>
                            <ComboBox x:Name="cmbDia" Width="130"
                                      Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,12">
                            <TextBlock Text="Entrada (HH:mm)" FontSize="11" Foreground="#546E7A" Margin="0,0,0,4"/>
                            <TextBox x:Name="txtEntrada" Width="110" Text="08:00"
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,12">
                            <TextBlock Text="Salida (HH:mm)" FontSize="11" Foreground="#546E7A" Margin="0,0,0,4"/>
                            <TextBox x:Name="txtSalida" Width="110" Text="17:00"
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                        </StackPanel>
                    </WrapPanel>
                    <WrapPanel Margin="0,4,0,0">
                        <StackPanel Margin="0,0,16,12">
                            <TextBlock Text="Vigente Desde" FontSize="11" Foreground="#546E7A" Margin="0,0,0,4"/>
                            <DatePicker x:Name="dpVigDesde" Width="150"
                                        Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,16,12">
                            <TextBlock Text="Vigente Hasta" FontSize="11" Foreground="#546E7A" Margin="0,0,0,4"/>
                            <DatePicker x:Name="dpVigHasta" Width="150"
                                        Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
                        </StackPanel>
                    </WrapPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button x:Name="btnCrear" Click="BtnCrear_Click"
                                Content="Crear Horario" Height="36" Padding="20,0" Margin="0,0,10,0"
                                Style="{StaticResource MaterialDesignFlatMidBgButton}"/>
                        <Button Content="Cancelar" Click="BtnCancelar_Click"
                                Height="36" Padding="20,0"
                                Style="{StaticResource MaterialDesignFlatButton}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- FEEDBACK -->
            <TextBlock x:Name="txtMensaje" Visibility="Collapsed"
                       FontSize="13" Margin="0,4,0,0" HorizontalAlignment="Center"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
